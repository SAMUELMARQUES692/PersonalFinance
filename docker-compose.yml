version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: Personal_Finance
    environment:
      POSTGRES_DB: personal_finance
      POSTGRES_USER: ${DATABASE_USERNAME}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
    ports:
      - "5431:5432"
    restart: always